#!/usr/bin/env python3
"""Just an add function"""


def add(a: float, b: float) -> float:
    """sums up two floating point numbers.

    Args:
        a (float): first number.
        b (float): second number.

    Returns:
        float: The summation result.
    """
    return a + b
